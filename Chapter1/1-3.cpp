int main()
{
    int x;          // defines a variable called x, which is an int
    double width;   // defines a variable called width, a double
    int a, b;       // defines two variables at once. Avoid for easier reading
}